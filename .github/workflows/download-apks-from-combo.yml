name: download-apks-from-combo

on:
  workflow_dispatch:
    inputs:
      package_name:
        description: "包名"
        required: true
        type: string
        default: "com.cxinventor.file.explorer;org.freedownloadmanager.fdm;com.rhmsoft.edit;com.rhmsoft.code;mark.via.gp;com.flyersoft.moonreader;com.azure.authenticator;org.mozilla.focus;com.mxtech.videoplayer.ad"
      architecture:
        description: "架构(universal等)"
        required: false
        default: "arm64-v8a"
        type: string

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@main
        with:
          path: ci_source

      - name: Generate timestamp
        run: echo "BUILD_DATE=$(TZ=UTC-8 date +'%y.%m.%d-%H.%M.%S')" >> $GITHUB_ENV

      - name: Setup node.js
        uses: actions/setup-node@main
        with:
          node-version: '20'

      - name: Install apkcombo-downloader
        run: |
          npm init -y
          npm install @nirewen/apkcombo-downloader

      - name: Prepare download configuration
        run: |
          set -euo pipefail
          DOWNLOAD_DIR="./apks-combo-${{ env.BUILD_DATE }}"
          mkdir -p "$DOWNLOAD_DIR"
          IFS=';' read -ra packages <<< "${{ inputs.package_name }}"
          echo "{" > apps.json
          echo '  "options": {' >> apps.json
          echo '    "arch": "${{ inputs.architecture }}",' >> apps.json
          echo '    "outDir": "'$DOWNLOAD_DIR'",' >> apps.json
          echo '    "dpi": "nodpi",' >> apps.json
          echo '    "fallbackArch": "universal"' >> apps.json
          echo '  },' >> apps.json
          echo '  "apps": [' >> apps.json
          first=true
          for pkg in "${packages[@]}"; do
            pkg_trimmed=$(echo "$pkg" | xargs)
            if [ -n "$pkg_trimmed" ]; then
              # 改进的 org 生成规则：提取包名中可能的应用名部分
              IFS='.' read -ra parts <<< "$pkg_trimmed"
              if [ ${#parts[@]} -ge 3 ]; then
                # 去掉前两个部分，剩余部分用连字符连接（例如 com.cxinventor.file.explorer → file-explorer）
                org=$(echo "${parts[@]:2}" | tr ' ' '-')
                # 特殊处理 com.google.android 类应用，应用名取最后一段
                if [[ "$pkg_trimmed" == com.google.android.* ]]; then
                  org="${parts[-1]}"
                fi
              else
                # 包名少于三段，直接取最后一段
                org="${parts[-1]}"
              fi
              repo="$pkg_trimmed"
              if [ "$first" = true ]; then
                first=false
              else
                echo "," >> apps.json
              fi
              echo '    {' >> apps.json
              echo '      "org": "'$org'",' >> apps.json
              echo '      "repo": "'$repo'",' >> apps.json
              echo '      "outFile": "'$pkg_trimmed'"' >> apps.json
              echo '    }' >> apps.json
            fi
          done
          echo '  ]' >> apps.json
          echo "}" >> apps.json
          cat apps.json

      - name: Download apks using node.js script
        run: |
          set -euo pipefail
          cat > download.js << 'EOF'
          const fs = require('fs');
          const { APKComboDownloader } = require('@nirewen/apkcombo-downloader');
          const config = JSON.parse(fs.readFileSync('apps.json', 'utf8'));
          const { options, apps } = config;
          const failedApps = [];
          (async () => {
            const downloader = new APKComboDownloader(options);
            for (const app of apps) {
              console.log(`正在下载：${app.org}/${app.repo}`);
              try {
                await downloader.download({
                  org: app.org,
                  repo: app.repo,
                  outFile: app.outFile
                });
                console.log(`✅ 下载成功：${app.outFile}`);
              } catch (err) {
                console.error(`❌ 下载失败：${app.org}/${app.repo} - ${err.message}`);
                failedApps.push(app);
              }
            }
            if (failedApps.length > 0) {
              console.error('\n以下应用下载失败：');
              failedApps.forEach(app => console.error(`- ${app.org}/${app.repo}`));
              process.exit(1);
            }
          })().catch(err => {
            console.error('脚本执行出错：', err);
            process.exit(1);
          });
          EOF
          node download.js
          echo "下载完成，文件清单："
          ls -lh "./apks-combo-${{ env.BUILD_DATE }}/"

      - name: Encrypt apks and create release
        uses: ./ci_source/.github/actions/aes256pac_release/
        with:
          ZIP_AES256_PWD: ${{ secrets.ZIP_AES256_PWD }}
          ARTIFACT_DIRECTORY_PATH: "./apks-combo-${{ env.BUILD_DATE }}"
          RELEASE_TAG_NAME: "${{ env.BUILD_DATE }}-from-combo"