name: build-apk-wadb

on:
  workflow_dispatch:
  workflow_call:

env:
  JDK_VERSION: '21'
  JDK_DISTRIBUTION: 'temurin'
  ANDROID_COMPILE_SDK: '36'
  ANDROID_BUILD_TOOLS: '36.0.0'
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clean runner environment
        run: |
          rm -rf ~/.gradle/caches/
          rm -rf ~/.gradle/daemon/
          rm -rf ~/.android/build-cache/
          rm -rf .gradle

      - name: Checkout source repo
        uses: actions/checkout@main
        with:
          repository: RikkaApps/WADB
          ref: master
          path: wadb_source
          fetch-depth: 0
          submodules: recursive

      - name: Checkout ci source repo (self)
        uses: actions/checkout@main
        with:
          path: ci_source

      - name: Initialization values
        run: |
          echo "BUILD_DATE=$(TZ=UTC-8 date +"%y.%m.%d-%H.%M.%S")" >> $GITHUB_ENV

      - name: Setup java
        uses: actions/setup-java@main
        with:
          distribution: '${{ env.JDK_DISTRIBUTION }}'
          java-version: '${{ env.JDK_VERSION }}'

      - name: Write keystore info
        working-directory: ./wadb_source
        run: |
          mkdir -p app
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > app/keystore.p12
          echo "KEYSTORE_FILE=./keystore.p12" >> signing.properties
          echo "KEYSTORE_PASSWORD=${{ secrets.KEYSTORE_PASSWORD }}" >> signing.properties
          echo "KEYSTORE_ALIAS=${{ secrets.KEY_ALIAS }}" >> signing.properties
          echo "KEYSTORE_ALIAS_PASSWORD=${{ secrets.KEY_ALIAS_PASSWORD }}" >> signing.properties

      - name: Setup gradle
        uses: gradle/actions/setup-gradle@main
        with:
          dependency-graph: generate-and-submit
          build-scan-publish: false

      - name: Build with gradle
        working-directory: ./wadb_source
        run: |
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null || true
          # 创建 local.properties 并设置 debug.dontRestartAdbd=false，避免 BuildConfig 错误
          echo "debug.dontRestartAdbd=false" > local.properties
          # 升级 material-preference 到 2.0.0（因为 1.1.0 不存在）
          echo "Upgrading dev.rikka.rikkax.material:material-preference to 2.0.0..."
          sed -i 's/dev\.rikka\.rikkax\.material:material-preference:1\.1\.0/dev.rikka.rikkax.material:material-preference:2.0.0/' app/build.gradle
          # 仅构建 arm64-v8a 架构
          sed -i "s/abiFilters .*/abiFilters 'arm64-v8a'/" app/build.gradle
          # 优化 Gradle 配置
          echo 'org.gradle.caching=true' >> gradle.properties
          echo 'org.gradle.parallel=true' >> gradle.properties
          chmod +x ./gradlew
          ./gradlew app:assembleRelease --stacktrace

      - name: List generated apk files
        working-directory: ./wadb_source
        run: |
          echo "Listing all apk files generated:"
          find app/build/outputs/apk -name "*.apk" | sort

      - name: Rename and move
        working-directory: ./wadb_source
        run: |
          mkdir -p "${{ github.workspace }}/dist"
          mv "./app/build/outputs/apk/release/"*release*.apk "${{ github.workspace }}/dist/wadb-${{ env.BUILD_DATE }}.apk"

      - name: Erase keystore, encrypt package and create release
        uses: ./ci_source/.github/actions/erase_aes256pac_release/
        with:
          P12_KEYSTORE_PATH: "./wadb_source/app/keystore.p12"
          ZIP_AES256_PWD: ${{ secrets.ZIP_AES256_PWD }}
          BUILD_ARTIFACT_PATH: "./dist/wadb-${{ env.BUILD_DATE }}.apk"
          RELEASE_TAG_NAME: "${{ env.BUILD_DATE }}-wadb"