name: build-apk-musicfree
 
on:
  workflow_dispatch:
  workflow_call:
 
env:
  JDK_VERSION: '17'
  JDK_DISTRIBUTION: 'temurin'
  ANDROID_COMPILE_SDK: '36'
  ANDROID_BUILD_TOOLS: '36.0.0'
  GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clean runner environment
        run: |
          rm -rf ~/.gradle/caches/
          rm -rf ~/.gradle/daemon/
          rm -rf ~/.android/build-cache/
          rm -rf .gradle
 
      - name: Checkout source repo
        uses: actions/checkout@main
        with:
          repository: maotoumao/MusicFree
          ref: dev
          path: musicfree_source
          fetch-depth: 0
          submodules: recursive
 
      - name: Checkout ci source repo (self)
        uses: actions/checkout@main
        with:
          path: ci_source
 
      - name: Initialization values
        run: |
          echo "BUILD_DATE=$(TZ=UTC-8 date +"%y.%m.%d-%H.%M.%S")" >> $GITHUB_ENV
 
      - name: Setup java
        uses: actions/setup-java@main
        with:
          java-version: "${{ env.JDK_VERSION }}"
          distribution: "${{ env.JDK_DISTRIBUTION }}"
 
      - name: Setup node.js
        uses: actions/setup-node@main
        with:
          node-version: 20
 
      - name: Install dependencies
        working-directory: ./musicfree_source
        run: |
          # 全局安装锁文件转换工具
          npm install -g synp
          # 将yarn.lock转换为npm可识别的package-lock.json
          synp --source yarn.lock --dest package-lock.json
          # 按转换后的锁文件安装依赖
          npm ci --prefer-offline --no-audit

      - name: Decode keystore.p12 and write signing.properties
        working-directory: ./musicfree_source/android
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > ./app/keystore.p12
          cat << 'EOF' > ./keystore.properties
          RELEASE_STORE_FILE=${{ github.workspace }}/musicfree_source/android/app/keystore.p12
          RELEASE_STORE_PASSWORD=${{ secrets.KEYSTORE_PASSWORD }}
          RELEASE_KEY_ALIAS=${{ secrets.KEY_ALIAS }}
          RELEASE_KEY_PASSWORD=${{ secrets.KEY_ALIAS_PASSWORD }}
          EOF
 
      - name: Build apk
        working-directory: ./musicfree_source/android
        run: |
          chmod +x ./gradlew
          ./gradlew assembleRelease --parallel --build-cache --configure-on-demand
 
      - name: List generated apk files
        working-directory: ./musicfree_source/android
        run: |
          echo "Listing all apk files generated:"
          find app/build/outputs -type f -name "*.apk" | sort
 
      - name: Rename and move
        working-directory: ./musicfree_source/android
        run: |
          mkdir -p "${{ github.workspace }}/dist"
          mv "./app/build/outputs/apk/release/app-arm64-v8a-release.apk" "${{ github.workspace }}/dist/musicfree-${{ env.BUILD_DATE }}.apk"
 
      - name: Erase keystore, encrypt package and create release
        uses: ./ci_source/.github/actions/erase_aes256pac_release/
        with:
          # 文件路径为基于github.workspace的绝对路径
          P12_KEYSTORE_PATH: "./musicfree_source/android/app/keystore.p12"
          ZIP_AES256_PWD: ${{ secrets.ZIP_AES256_PWD }}
          BUILD_ARTIFACT_PATH: "./dist/musicfree-${{ env.BUILD_DATE }}.apk"
          RELEASE_TAG_NAME: "${{ env.BUILD_DATE }}-musicfree"