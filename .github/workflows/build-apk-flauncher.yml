name: build-apk-flauncher

on:
  workflow_dispatch:
  workflow_call:

env:
  JDK_VERSION: '21'
  ANDROID_COMPILE_SDK: '36'
  ANDROID_BUILD_TOOLS: '36.0.0'
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clean runner environment
        run: |
          rm -rf ~/.gradle/caches/
          rm -rf ~/.gradle/daemon/
          rm -rf ~/.android/build-cache/
          rm -rf .gradle

      - name: Checkout source repo
        uses: actions/checkout@main
        with:
          repository: osrosal/flauncher
          ref: master
          path: flauncher_source
          fetch-depth: 0
          submodules: recursive

      - name: Checkout ci source repo (self)
        uses: actions/checkout@main
        with:
          path: ci_source

      - name: Initialization values
        run: |
          echo "BUILD_DATE=$(TZ=UTC-8 date +"%y.%m.%d-%H.%M.%S")" >> $GITHUB_ENV

      - name: Set version variables
        id: version
        working-directory: ./flauncher_source
        run: |
          cat version_numbers >> $GITHUB_OUTPUT
          export $(cat version_numbers | xargs)
          echo "FULL_VERSION_NUMBER=$(date '+%Y.%m').$(printf '%03d' $MONTH_VERSION_NUMBER)" >> $GITHUB_OUTPUT

      - name: Setup java
        uses: actions/setup-java@main
        with:
          distribution: 'temurin'
          java-version: '${{ env.JDK_VERSION }}'

      - name: Install flutter
        uses: subosito/flutter-action@main
        with:
          # flutter-version: '3.24.5'
          flutter-version: '3.41.2'
          channel: 'stable'

      - name: Accept android licenses
        working-directory: ./flauncher_source
        run: yes | flutter doctor --android-licenses || true

      - name: Write keystore info
        working-directory: ./flauncher_source
        run: |
          mkdir -p android/app
          # 实际为p12，非jks，懒得改构建代码了
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android/app/upload-keystore.jks

      - name: Patch gradle files for latest flutter version
        working-directory: ./flauncher_source
        run: |
          set -x
          # 定义目标文件
          APP_BUILD_GRADLE="android/app/build.gradle"
          ROOT_BUILD_GRADLE="android/build.gradle"
          GRADLE_PROPERTIES="android/gradle/wrapper/gradle-wrapper.properties"
          # 检查关键文件是否存在
          for f in "$APP_BUILD_GRADLE" "$GRADLE_PROPERTIES"; do
            if [ ! -f "$f" ]; then
              echo "❌ 错误：找不到 $f"
              exit 1
            fi
          done
          # 1. 升级 Gradle 包装器到 8.11.1（兼容 AGP 8.6.0）
          sed -i 's|distributionUrl=.*gradle-[0-9.]*-all\.zip|distributionUrl=https\://services.gradle.org/distributions/gradle-8.11.1-all.zip|' "$GRADLE_PROPERTIES"
          # 2. 在 splits.abi 块中添加 include 指令（如果尚未存在）
          if ! grep -q "include.*arm64-v8a" "$APP_BUILD_GRADLE"; then
            # 使用 awk 在 enable 行后插入 include
            awk '/splits\s*{\s*abi\s*{/,/}/ { 
              if ($0 ~ /enable[^\n]*/) { 
                print $0; 
                print "            include \"arm64-v8a\", \"armeabi-v7a\", \"x86\", \"x86_64\"";
              } else print $0; 
              next 
            } 1' "$APP_BUILD_GRADLE" > "$APP_BUILD_GRADLE.tmp" && mv "$APP_BUILD_GRADLE.tmp" "$APP_BUILD_GRADLE"
          fi
          # 3. 可选：如果 Kotlin 版本存在，升级到 1.9.20
          if grep -q 'ext.kotlin_version' "$ROOT_BUILD_GRADLE" 2>/dev/null; then
            sed -i 's/ext.kotlin_version = ["'\'']*[0-9.]*["'\'']*/ext.kotlin_version = "1.9.20"/' "$ROOT_BUILD_GRADLE"
          fi
          # 4. 显示修改结果
          echo "Gradle 文件已更新："
          echo "Gradle 包装器版本："
          grep "distributionUrl" "$GRADLE_PROPERTIES"
          echo "ABI splits 配置："
          sed -n '/splits\s*{\s*abi\s*{/,/}/p' "$APP_BUILD_GRADLE"

      - name: Build with flutter
        working-directory: ./flauncher_source
        env:
          FLAUNCHER_KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          FLAUNCHER_SIGNING_KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          FLAUNCHER_SIGNING_KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          BUILD_NUMBER: ${{ steps.version.outputs.BUILD_NUMBER }}
          FULL_VERSION_NUMBER: ${{ steps.version.outputs.FULL_VERSION_NUMBER }}
        run: |
          echo 'org.gradle.caching=true' >> android/gradle.properties
          echo 'org.gradle.parallel=true' >> android/gradle.properties
          flutter --no-color build apk --release \
            --build-number $BUILD_NUMBER \
            --build-name=$FULL_VERSION_NUMBER

      - name: Rename and move
        working-directory: ./flauncher_source
        run: |
          mkdir -p "${{ github.workspace }}/dist"
          mv "./build/app/outputs/flutter-apk/app-arm64-v8a-release.apk" "${{ github.workspace }}/dist/flauncher-${{ env.BUILD_DATE }}.apk"
 
      - name: Erase keystore, encrypt package and create release
        uses: ./ci_source/.github/actions/erase_aes256pac_release/
        with:
          P12_KEYSTORE_PATH: "./flauncher_source/android/app/upload-keystore.jks"
          ZIP_AES256_PWD: ${{ secrets.ZIP_AES256_PWD }}
          BUILD_ARTIFACT_PATH: "./dist/flauncher-${{ env.BUILD_DATE }}.apk"
          RELEASE_TAG_NAME: "${{ env.BUILD_DATE }}-flauncher"