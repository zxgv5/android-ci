name: build-apk-btremote

on:
  workflow_dispatch:
    inputs:
      branch_name:
        description: 'Branch to build from'
        required: false
        default: 'main'
        type: string
      release_type:
        description: 'Release type: play (Google Play) or fdroid (F-Droid)'
        required: false
        default: 'fdroid'
        type: choice
        options:
          - play
          - fdroid

env:
  JDK_VERSION: '21'
  JDK_DISTRIBUTION: 'temurin'
  ANDROID_COMPILE_SDK: '36'
  ANDROID_BUILD_TOOLS: '36.0.0'
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clean runner environment
        run: |
          rm -rf ~/.gradle/caches/
          rm -rf ~/.gradle/daemon/
          rm -rf ~/.android/build-cache/
          rm -rf .gradle

      - name: Checkout source repo
        run: |
          git clone --branch ${{ github.event.inputs.branch_name }} https://gitlab.com/Atharok/BtRemote.git btremote_source
          cd btremote_source
          git submodule update --init --recursive

      - name: Checkout ci source repo (self)
        uses: actions/checkout@main
        with:
          path: ci_source

      - name: Initialization values
        run: |
          echo "BUILD_DATE=$(TZ=UTC-8 date +"%y.%m.%d-%H.%M.%S")" >> $GITHUB_ENV

      - name: Setup java
        uses: actions/setup-java@main
        with:
          distribution: '${{ env.JDK_DISTRIBUTION }}'
          java-version: '${{ env.JDK_VERSION }}'

      - name: Write keystore info
        working-directory: ./btremote_source
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > ${{ github.workspace }}/btremote_source/keystore.p12
          echo "ANDROID_KEY_STORE_FILE=${{ github.workspace }}/btremote_source/keystore.p12" >> $GITHUB_ENV
          echo "ANDROID_KEY_STORE_PASSWORD=${{ secrets.KEYSTORE_PASSWORD }}" >> $GITHUB_ENV
          echo "ANDROID_KEY_ALIAS=${{ secrets.KEY_ALIAS }}" >> $GITHUB_ENV
          echo "ANDROID_KEY_PASSWORD=${{ secrets.KEY_ALIAS_PASSWORD }}" >> $GITHUB_ENV

      - name: Add signing config to release build type
        working-directory: ./btremote_source
        run: |
          sed -i '/release {/a \            signingConfig = signingConfigs.getByName("release")' app/build.gradle.kts
          echo "Updated app/build.gradle.kts:"
          cat app/build.gradle.kts | grep -A5 "release {"

      - name: Setup gradle
        uses: gradle/actions/setup-gradle@main
        with:
          dependency-graph: generate-and-submit
          build-scan-publish: true
          build-scan-terms-of-use-url: "https://gradle.com/terms-of-service"
          build-scan-terms-of-use-agree: "yes"

      - name: Build with gradle
        working-directory: ./btremote_source
        run: |
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null || true
          echo "" >> gradle.properties
          echo 'org.gradle.caching=true' >> gradle.properties
          echo 'org.gradle.parallel=true' >> gradle.properties
          chmod +x ./gradlew
          if [ "${{ github.event.inputs.release_type }}" == "play" ]; then
            ./gradlew app:assembleGplayRelease -Pandroid.abis=arm64-v8a
          else
            ./gradlew app:assembleDefaultRelease -Pandroid.abis=arm64-v8a
          fi

      - name: List generated apk files
        working-directory: ./btremote_source
        run: |
          echo "Listing all apk files generated:"
          find app/build/outputs -name "*.apk" | sort

      - name: Rename and move
        working-directory: ./btremote_source
        run: |
          mkdir -p "${{ github.workspace }}/dist"
          if [ "${{ github.event.inputs.release_type }}" == "play" ]; then
            mv "./app/build/outputs/apk/gplay/release/app-gplay-release.apk" "${{ github.workspace }}/dist/btremote-${{ env.BUILD_DATE }}.apk"
          else
            mv "./app/build/outputs/apk/default/release/app-default-release.apk" "${{ github.workspace }}/dist/btremote-${{ env.BUILD_DATE }}.apk"
          fi

      - name: Erase keystore, encrypt package and create release
        uses: ./ci_source/.github/actions/erase_aes256pac_release/
        with:
          P12_KEYSTORE_PATH: "./btremote_source/keystore.p12"
          ZIP_AES256_PWD: ${{ secrets.ZIP_AES256_PWD }}
          BUILD_ARTIFACT_PATH: "./dist/btremote-${{ env.BUILD_DATE }}.apk"
          RELEASE_TAG_NAME: "${{ env.BUILD_DATE }}-btremote"
